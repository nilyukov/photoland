import Hero from '../sections/Hero';
import LatestProducts from '../sections/LatestProducts';

const Home = () => {
    return (
        <main>
            <Hero />
            <LatestProducts />
        </main>
    );
};

export default Home;
